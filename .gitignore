*.slo
*.lo
*.o
*.obj
*.gch
*.pch
*.so
*.dylib
*.dll
*.a
*.lib
*.exe
*.out
*.app
.vs
.vscode
.idea/

build/
cmake-build-*/
out/
.cache/
bin/TesseraEmu_*
bin/TesseraEmu_*.exe
# bundle builds: the .app is covered by *.app above, but the DMG that CI and a
# local MACOS_BUNDLE=ON build produce is not, and it is 29 MB
bin/*.dmg
bin/dmg/

# TesseraEmu bin files
bin/otp.bin
bin/seeprom.bin
bin/log.txt
bin/TesseraEmu_*.pdb
bin/TesseraEmu_*.ilk
bin/TesseraEmu.exe.backup
bin/mlc01/*
bin/settings.xml
bin/network_services.xml
bin/title_list_cache.xml
bin/debugger/*
bin/sdcard/*
bin/screenshots/*
bin/dump/*
bin/cafeLibs/*
bin/portable/*
bin/keys.txt

!bin/shaderCache/info.txt
bin/shaderCache/*

bin/controllerProfiles/*

!bin/gameProfiles/default/*
bin/gameProfiles/*

bin/graphicPacks/*

# Ignore Finder view option files created by OS X
.DS_Store
# local test content (game images) - never commit
/Roms/
# user game saves -- never commit
/Saves/

/build*/

# a global gitignore aimed at JS build output ignores dist/ everywhere, which
# silently kept dist/macos/*.entitlements out of the repo -- the files the
# bundle is signed with. A repo rule outranks the global one, so re-include it.
!dist/

# cemu-re-mcp local venv (bootstrap with tools/cemu-re-mcp/setup.sh)
tools/cemu-re-mcp/.venv/

# tessera-playtest MCP venv
tools/tessera-playtest/.venv/
