"""cemu_re_mcp — MCP server for Cemu GDB stub.

Top-level package. Most users want `cemu_re_mcp.server:main` as the entrypoint
(installed as the `cemu-re-mcp` console script).
"""
__version__ = "0.0.1"
